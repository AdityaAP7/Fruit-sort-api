// app.js
// app.js
// app.js

const express = require('express');
const app = express();

const fruitRoutes = require('./routes/fruitRoutes');

// Mount the fruitRoutes under the '/fruits' path

app.use('/', fruitRoutes);


// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000');
});

const express = require('express');
const app = express();

const fruitRoutes = require('./routes/fruitRoutes');

// Mount the fruitRoutes under the '/fruits' path

app.use('/', fruitRoutes);


// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000');
});

const express = require('express');
const app = express();

const fruitRoutes = require('./routes/fruitRoutes');

// Mount the fruitRoutes under the '/fruits' path

app.use('/', fruitRoutes);


// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000');
});


























// app.js

const express = require('express');
const app = express();

const fruitRoutes = require('./routes/fruitRoutes');

// Mount the fruitRoutes under the '/fruits' path

app.use('/', fruitRoutes);


// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000');
});
